# backend/requirements.txt — ОЧИЩЕННЫЙ ВАРИАНТ
# === Core ===
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# === Database ===
sqlalchemy[asyncio]==2.0.25
asyncpg==0.29.0
alembic==1.13.1
psycopg2-binary==2.9.9

# === Security ===
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.6

# === Tasks & Cache ===
celery==5.3.6
redis==5.0.1

# === Utilities ===
httpx==0.26.0
openpyxl==3.1.2
pandas==2.2.0

# === LLM (in-process, on-prem; читает models/llm/asok-model.gguf) ===
# CPU-сборка llama.cpp. Модель — Qwen2.5-1.5B-Instruct Q4_K_M (стандартная архитектура GGUF
# `qwen2`, поддерживается этой версией рантайма; ~1 ГБ, помещается в лимит памяти контейнера).
# ВНИМАНИЕ: модель с архитектурой `qwen35` (Qwen3.5) этой и любой текущей стабильной сборкой
# llama.cpp НЕ загружается ("unknown model architecture: 'qwen35'"). Для более новых архитектур
# (qwen3) требуется llama-cpp-python>=0.3.x; arch `qwen35` нестандартен и непереносим.
llama-cpp-python==0.2.90

# === Dev (опционально для контейнера) ===
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
