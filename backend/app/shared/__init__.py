"""
shared/ — общее ядро без бизнес-логики конкретного домена (ТЗ v13).

Сюда попадает ТОЛЬКО действительно общее и стабильное:
    db.py          — общие ORM-миксины (TimestampMixin, SoftDeleteMixin)
    exceptions.py  — базовые доменные исключения (DomainError, NotFound, Conflict)
    types.py       — общие типы/алиасы/перечисления кросс-домена
    ports.py       — протоколы внешних интеграций (СУЗ/ТМС/ITSM/DWH) — КОНТРАКТЫ

shared НЕ импортирует modules и не тянет инфраструктуру (кроме типовых зависимостей).
Реализации портов живут в infrastructure/integrations; здесь — только интерфейсы.
"""
