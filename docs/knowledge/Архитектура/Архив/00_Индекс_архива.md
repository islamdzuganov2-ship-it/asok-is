---
tags: [асок-ис, архитектура, архив, historical]
date: 2026-06-27
status: archived
---

# Архив удалённого кода (исторические данные)

> Здесь хранится код, **удалённый из кодовой базы** как мёртвый/дублирующий (см. аудит
> [[04_Зависимости]] §5). Сохранено как исторические данные. Полная версия с историей —
> в git до коммита удаления от 2026-06-27.

## Содержимое
- [[Frontend_удалённый_код]] — фронтенд: AiInsightBanner, axiosInstance, authApi, mockExcelData,
  битый дубль `app/App.tsx`, пустые заглушки.
- [[Backend_удалённый_код]] — бэкенд: calculator, core/rbac, services/{assessment,system,auth,templates}.
- [[mockDashboards_удалённые_значения]] — демо-значения EXECUTIVE_MOCK / MANAGER_MOCK / MANAGER_MOCK_SYSTEMS
  (вытеснены `data/mockScaleData.ts`; типы из mockDashboards оставлены живыми).

## Причина удаления
Подтверждено по импортам: ни один из файлов не подключён к рабочему графу
(точка входа `src/App.tsx`, активный RBAC `api/deps.require_role`, актуальный расчёт
`calculation_engine.py`, актуальные схемы `app/schemas/*`).
