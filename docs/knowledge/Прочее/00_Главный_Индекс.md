---
tags:
  - фронт
  - бэк
aliases:
  - АСОК ИС
  - asok-is
  - главная
---

# 🏦 АСОК ИС — Главный индекс

> Автоматизированная Система Оценки Качества Информационных Систем банка.
> Методология МК_8.1 (адаптация ISO 25010), 28 метрик, 9 характеристик.

---

## 📌 Быстрый старт (восстановление контекста)

```
Репо:    https://github.com/islamdzuganov2-ship-it/asok-is
Коммит:  1 (все правки локальные, не залиты в git)
Backend: http://localhost:8000   (FastAPI)
Frontend: http://localhost:3000  (Vite + React)
Swagger: http://localhost:8000/docs
```

**Демо-логины:**
| username | password | role |
|---|---|---|
| demo | manager | QUALITY_MANAGER |
| admin | Admin123! | ADMIN |
| analyst | Analyst123! | TEST_ANALYST |
| cto | cto | CTO |

---

## 🗂️ Карта знаний

```
[[АСОК_ИС_Архитектура]]          ← стек, взаимосвязи, data flow
[[АСОК_ИС_Backend]]              ← все backend файлы, endpoints, модели
[[АСОК_ИС_Frontend]]             ← все frontend файлы, компоненты, страницы
[[АСОК_ИС_DevOps]]               ← docker-compose, CI/CD, миграции
[[АСОК_ИС_Статус_и_Бэклог]]      ← текущие проблемы, что сделать
[[АСОК_ИС_Code_Reviews]]         ← история ревью v1–v7
[[АСОК_ИС_Восстановление]]       ← промпт для новой сессии
```

---

## ⚡ Команды запуска

```powershell
# Полный старт с нуля
docker compose down -v
docker compose up -d --build
Start-Sleep -Seconds 25

# Инициализация БД
docker exec asok_backend python app/init_db.py
docker exec asok_backend python -m app.scripts.seed_metrics
docker exec asok_backend python -m app.scripts.seed_demo

# Проверка
Invoke-RestMethod "http://localhost:8000/health"
```

---

## 🔴 Критические задачи прямо сейчас

1. `git add . && git commit -m "fix: v7 fixes" && git push`
2. `docker exec asok_backend python app/init_db.py`
3. Исправить [[АСОК_ИС_Backend#auth.py]] — JSON вместо form-data
4. Подключить [[АСОК_ИС_Frontend#DashboardPage]] к `/api/v1/assessments/dashboard`


---

## 🧭 Методология LLM

- [[09_Манифест_LLM_Дао-Тойота]] — многоаспектное рассуждение LLM (Дао Тойота × ISO 25010/38500):
  конвейер Генти-Генбуцу → 5 Почему → Немаваси (ролевые линзы) → Дзидока → Хансей → заключение ЛПР.
  Реализация — `modules/llm/reasoning.py` (BL‑005).
- [[09_Манифест_LLM_Дао-Тойота#ЧАСТЬ VI. Обновление знаний по обучению LLM|Обновление знаний обучения]]
  — SFT-корпус теперь несёт трассу рассуждения, а не только output.


---

## Обновление 2026-07-06 — ТЗ v14: согласование ТЗ и кода

- [[02_ТЗ_v14_Динамика_Аномалии_Аудит]] — динамика качества (карточка «Качество ИС» по
  кварталам, все системы, аномалии с причинами МК + уведомления), связь суждений с
  характеристикой, правки мер топ-менеджером с аудитом, ролевые данные, сквозная проливка.
  **Реализовано и протестировано** (124 backend + 14 frontend тестов).
- [[10_Заключение_по_данным_2026-07-06]] — анализ всех тестовых данных + инструкция LLM.
- [[Отчёт_о_тестировании_АСОК_ИС_2026-07-06]] — отчёты (функц. + польз.).
- Демо-логин топ-менеджера: **cto / Cto12345!** (роль CTO).
- Метки **#фронт / #бэк** проставлены по всем заметкам кода АСОК (frontmatter tags).
- Задача #18 (удаление shim-слоёв) выполнена ранее и подтверждена: стек и 124 теста зелёные.
