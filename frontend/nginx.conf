server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # Поддержка React Router (перенаправление всех запросов на index.html)
    location / {
        try_files $uri $uri/ /index.html;
    }
}